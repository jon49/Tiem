// Generated by LiveScript 1.2.0
var ClockState, keys;
ClockState = require('./../models/ClockState');
keys = require('./../constants/object-keys');
describe('How ClockState model is used:', function(){
  describe('The function `create`', function(){
    var x$;
    x$ = it;
    x$('should create a new ClockState plain object', function(){
      var newDate;
      newDate = new Date();
      expect(keys.clockStateKeys.length).toEqual(2);
      expect(ClockState.create(newDate, Option.Some(newDate))).toEqual({
        'in': newDate,
        'out': Option.Some(newDate, {
          ctor: 'ClockState'
        })
      });
    });
  });
});